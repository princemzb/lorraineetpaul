DATABASE_URL=postgresql://mariage:mariage_secret@localhost:5432/mariage
# Generate a real secret before deploying: openssl rand -base64 32
AUTH_SECRET=CHANGE_ME_generate_with_openssl_rand_base64_32
NEXTAUTH_URL=http://localhost:3000
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
MAIL_FROM="Loraine & Paul <noreply@mariage-loraine-paul.fr>"
APP_URL=http://localhost:3000
# Adresse recevant les messages du formulaire /contact (optionnel, sinon
# retombe sur mazabaprince@gmail.com — voir src/lib/email.ts)
CONTACT_EMAIL=
# Identifiants admin créés par le seed. À définir en production — sans ça,
# retombe sur admin@mariage.fr / password123 (dev uniquement).
ADMIN_EMAIL=
ADMIN_PASSWORD=
